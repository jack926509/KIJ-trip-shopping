# UX/UI 改版計畫（2026-08-12 建立）

## 背景

線上版桌機表現尚可，但手機版版面崩壞。使用者（非工程師）將在北九州旅途中以手機為主要載具使用本站。

基準：`4f37dc9`（本機於 2026-08-12 22:20 由 origin/main 快轉更新，先前落後 17 個 commit）。
基準線驗收：`npm test` ✓、`npm run validate:data` ✓（49 項商品）。

## 使用情境（2026-08-12 使用者確認，三者皆要）

1. **在店裡邊逛邊用**（主要）：單手、站著。要快速勾選、看價格、確認店家。
2. **行前規劃與比價**：分類瀏覽、總金額試算。
3. **到店前查「這東西在哪買」**：商品與地圖互查。

## 改版範圍（2026-08-12 使用者確認）

手機優先重做版面，**保留現有配色與整體調性**（米白底 `#f4f1eb`、深綠 `#2f6d5e`）。不換色票、不換字型。

## 核心問題（實測線上版 390×844 與 1280×900）

| 編號 | 問題 | 證據 |
|---|---|---|
| P1 | 手機卡片版面崩壞：`.mid` 僅剩約 110px，`.kij-shopping-details` 的 70px 標籤欄再切掉大半，店家名變一字一行 | `assets/kij.css:353-368`、`assets/kij.css:326-335` |
| P2 | 「已購買」按鈕浮在卡片右側中段，與左欄對不齊 | `assets/kij.css:290-298` |
| P3 | 頁首 sticky 區塊佔手機螢幕約 22% | `assets/kij.css:224-252` |
| P4 | 進度條不是進度條，只有一個大空白框 | `assets/kij.css:239-248` |
| P5 | 「共 49 項」與「已買 0/39」並列無說明（49＝全部含比較參考、39＝`tracking:'buy'`），使用者只會覺得數字對不上 | `index.html:421`、`index.html:556` |
| P6 | 商品與地圖無雙向連結，情境 3 無法達成 | 全站無此功能 |
| P7 | `map.html` 內嵌 566 行 style／680 行 script，與首頁視覺語言不一致（原生下拉 vs pill、返回連結 vs 底部導覽） | `map.html` |
| P8 | 地圖頁手機版：頁首＋說明＋篩選佔逾半螢幕，地圖本體僅約 380px；圖例右側「食品」被切掉 | 實測截圖 |
| P9 | `index.html` 與 `kij.css` 全無 `:focus-visible` 樣式 | `assets/kij.css` 全檔無 `focus` |
| P10 | 手機數量按鈕 32px，低於自訂 `--tap-min: 44px` | `assets/kij.css:363` |
| P11 | 無 favicon（線上 404） | 實測 console |
| P12 | `:root` 的暖白珊瑚橘色票是死碼，被 `body.kij` 覆寫 | `assets/kij.css:2-14` vs `207-217` |

## 薄切片與驗收條件（白話）

### 切片 1：手機卡片改直式版面（P1、P2、P10）
**做完你會看到**：手機打開清單，每張商品卡片是「上面照片＋品名，中間價格與數量，下面一整條『已購買』按鈕」，沒有任何文字被擠成直排一字一行；店家名一行讀得完或正常換行。加減數量的按鈕手指按得到（至少 44×44）。
**機械驗收**：390px 寬下 `.kij-shopping-details dd` 的實際寬度 > 160px；`.kij-qty-btn` 實際尺寸 ≥ 44px；`npm test` 綠燈。

### 切片 2：已購買狀態化（P2）
**做完你會看到**：勾了「已購買」的商品整張卡片變淡、品名加刪除線，掃一眼就知道還剩哪些沒買。
**機械驗收**：勾選後卡片有 `is-bought` 狀態且 `opacity` 下降；重新整理後狀態保留。

### 切片 3：頁首瘦身＋數字說明（P3、P5）
**做完你會看到**：頁首變矮，往下滑時標題收合，看得到更多商品；「已買 x / 39」旁邊註明另 10 項為比較參考，講清楚跟「共 49 項」的差別。
**機械驗收**：390px 下頁首高度 ≤ 160px。

> 2026-08-12 使用者決定：**不要進度條、不要金額試算**（P4 撤銷）。原本做好的進度條與待買金額已移除，只留一行狀態文字。

### 切片 3b：卡片改「大圖在左、已購買釘右上」（使用者指定版型）
**做完你會看到**：照片放大並在左側上下置中；分類標籤在右上區左側、已購買勾選釘在卡片右上角；品名本身就是商品頁超連結（點品名開來源頁）；數量旁即時顯示小計。
**機械驗收**：390px 下明細值欄 > 160px、加減鈕高 ≥ 44px；圖片上下留白差 ≤ 2px（桌機）；品名連結帶 `rel="noopener noreferrer"`；`npm test` 綠燈。

### 切片 4：商品 ↔ 地圖雙向連結（P6）
**做完你會看到**：卡片上的店家名可以點，點了跳到地圖並標出那家店；地圖上點店家會顯示「這家店有你清單上的 N 項」。
**機械驗收**：卡片店家連結帶 `?store=<id>`，地圖載入後該店 marker 為選中狀態。

### 切片 5：地圖頁重排並與首頁統一（P7、P8）
**做完你會看到**：手機打開地圖，地圖本身佔大半螢幕；篩選壓成一列；圖例可左右滑，不會被切掉；底部導覽與首頁一致。
**機械驗收**：390px 下地圖容器高度 ≥ 螢幕 55%；圖例容器 `scrollWidth > clientWidth` 時可捲動且無裁切。

### 切片 6：收尾（P9、P11、P12）
**做完你會看到**：用鍵盤 Tab 移動時看得到目前focus在哪；瀏覽器分頁有圖示。
**機械驗收**：`:focus-visible` 樣式存在；`/favicon.ico` 不再 404；死碼色票清除後畫面不變。

## 驗收方式

每切片：`npm test` ＋ `npm run validate:data` 綠燈 → headless Chrome 實測 390px 與 1280px 截圖 → 全部切片完成後派 fresh-context subagent 交叉審查 diff。

## 進度

- [x] 切片 1
- [x] 切片 2
- [x] 切片 3（進度條與金額試算依使用者指示撤銷）
- [x] 切片 3b
- [x] 切片 4
- [x] 切片 5
- [x] 切片 6

## 實作補充（2026-08-12 完成切片 4–6 時的實際做法）

**切片 4**：`map.html` 原本就有 `applyStoreUrlParam()`，缺的是清單頁那一半。
做法：`index.html` 的「推薦店家」由純文字改為 `.kij-store-chip` 連結（`map.html?store=<id>`）；
地圖頁店家卡片與圖釘彈窗顯示「你的清單 N 項・還沒買 M 項」，已買的商品縮圖轉灰。
未購買數只計 `tracking === 'buy'` 的品項——鞋款是到店試穿，沒有「買了沒」的狀態。

**切片 5**：分類篩選由 `<select>` 改為 `data-category` pill（沿用既有 `applyFilters` 的泛型邏輯）；
說明文字收進 `<details>`；手機頁首去掉白底卡片；`#map` 提高到 58vh；
`.filter-group`／`.legend` 捲得動時才加 `.is-scrollable`（右緣漸層淡出）。
另修兩個既有缺陷：(a) 手機 `.map-panel { position: static }` 讓 `.map-attribution`
掉到 body 座標系、疊在底部導覽上，改為 `relative`；(b) `.kij-bottombar .active` 用 `var(--accent)`，
但 `map.html` 未定義該變數而掉回 `kij.css` 的珊瑚橘，導致地圖頁選中色與清單頁不同。

**切片 6**：P12 已確認 `map.html` 自帶完整 `:root` 色票、不依賴 `kij.css` 的珊瑚橘，
故直接把 `kij.css` 的 `:root` 換成深綠色票並刪除 `body.kij` 的重複覆寫（值完全相同）；
`:focus-visible` 以 `:where()` 零特異度寫在 `kij.css`，兩頁共用；favicon 用 `favicon.svg`（購物袋＋勾）。

## 對抗式審查與修正（2026-08-13）

fresh-context opus agent 審查 `git diff 4f37dc9..b757936`（index.html、map.html、kij.css、favicon.svg），
提出 8 個 finding，全數已修（commit `6afde83`、`338dcf1`）：

| 編號 | 問題 | 修法 |
|---|---|---|
| F1（高）| `?store=` 指到「篩完只剩一家、且遠離博多」的店時，地圖不動、圖釘落在畫面外。原因是 `initMap()` 的第一次 `fitBounds` 把縮放動畫排到下一個 frame，同 tick 後續的 `setView` 被覆蓋 | 拆成 `readStoreUrlParam()`，在建立地圖「之前」就設好篩選，讓第一次框選直接框對 |
| F2 | 小倉提示語與 `renderMarkers` 的框選邏輯各寫一份，日後新增「小倉獨有分類」的店就會顯示相反的話 | 抽成共用的 `framedStores()`，提示條件改為 `framed.length !== stores.length` |
| F3 | iOS Safari 按上一頁走 bfcache，script 不重跑，「還沒買 N 項」是舊的 | 補 `pageshow` handler，`persisted` 時重新 `renderStores()` |
| F4 | `.kij-store-chip` 只有 34px | 提到 `--tap-min`（44px） |
| F5 | 右緣淡出固定掛著，捲到底時最後一顆 pill 永遠是淡的（而 `revealActiveFilter` 正好常把選中那顆送到最右）| 改看剩餘可捲距離，並掛 scroll 事件 |
| F6 | `.kij-card` 軌道宣告 108px 但 `.thumb` 是 118px，圖片溢出軌道、實際間距只剩 2px | 軌道對齊 118px（實測回到 12px）|
| F7 | `.filter-group button` 的 38px 在 541–820px 生效，手機轉橫時過小 | 一律用 `--tap-min` |
| F8 | `.kij-map-open` 死碼（改版前既有）| 整組刪除 |

審查另確認：Leaflet CDN 的 SRI 雜湊實算相符、favicon 200、無孤兒 CSS 選擇器、
`localStorage` 四個存取點皆有 try/catch（無痕模式不會壞）、`aria-pressed`／`aria-live` 與實際狀態同步。

順帶修正（非審查發現）：
- 便利商店群組原本一律不顯示價格，改為「查得到價的就顯示」（6 項中 4 項有價）。
- 店家卡片的「Google Maps 導航」等 `.action-link` 由 36px 提到 44px。

## 已知未處理（留給使用者決定）

- 便利商店群組的 `priceSourceUrl` 指向 prtimes.jp 新聞稿，不是真正商品頁。
- 55 項中有數項沒有 `priceSourceUrl`，品名維持純文字、無連結（有連結的品名帶 ↗ 記號）。
- 有 4 個商品引用的店家 id 不在 `STORE_SUMMARIES`（`biccamera-tenjin-1`、`lawson-hakata`、
  `lawson-nishitetsu-fukuoka-tenjin-south`、`lawson-kokura-uomachi-2chome`），
  這些不會產生店家標籤。此行為改版前就存在，非回歸，但資料面可補。
