# UX/UI 優化第二輪（2026-08-26）

## 背景

第一輪（`PLAN-uxui-2026-08-12.md`）已完成並上線。之後商品由 49 項成長到 **75 項**、
分頁 6 個，並新增了 `itinerary.html`。以 390×844 與 1280×900 實測目前線上版本後，
本輪針對「站在店裡單手操作」這個主要情境再做一輪優化。

基準：`deb3210`。基準線驗收：`npm test` ✓、`npm run validate:data` ✓（75 項商品、46 家店）。

## 本輪問題（實測 390×844 與 1280×900）

| 編號 | 問題 | 證據 |
|---|---|---|
| Q1 | **清單頁沒有搜尋**。75 項商品散在 6 個分頁，站在藥妝店貨架前只能一路捲。地圖頁反而早就有搜尋列，兩頁能力不對等 | `map.html:1122`（`#storeSearch`）vs `index.html` 無 |
| Q2 | **沒有「只看未完成」**。買到後半段時，畫面仍被已買的卡片佔滿，要一直略過 | 全站無此功能 |
| Q3 | 底部導覽 `rgba(255,255,255,.96)` 半透明且無模糊，捲動時內文透出來疊字 | `assets/kij.css` `.kij-bottombar` |
| Q4 | 桌機 1280px 仍是滿版手機式底部列，橫跨整個螢幕壓在內容上 | 實測截圖 |
| Q5 | 藥妝日用分區：分類已是小標題，每張卡片右上又重複一次同樣的分類膠囊，把 172px 的右欄再切掉一截 | `buildSectionHtml` 的 `.kij-subcat-heading` 與 `pillForProduct` |
| Q6 | `priceNote`（多為「官方含稅價…實體藥妝售價與庫存未確認」這類來源免責說明）每張卡片都攤開，佔的高度常比價格還多，在店裡是雜訊 | `buildCardHtml` |
| Q7 | 行程頁日期分頁（白底描邊）與清單頁分頁（米色膠囊）視覺語言不同；捲到當日後半段時日期分頁已捲出畫面 | `assets/itinerary.css` `.itinerary-day-tab` |
| Q8 | 行程頁看不出「今天是哪一天」，四個日期分頁一樣重 | `itinerary-app.js` `defaultDate()` 有算，畫面沒標 |
| Q9 | 手機版行程頁每個停靠點的「導航」按鈕獨佔一整列，一天十幾站就多出十幾列 | `assets/itinerary.css:@media(max-width:560px)` |
| Q10 | 分頁按鈕沒有任何選中狀態的無障礙標記 | `index.html` `.kij-tab` |

## 切片與驗收條件

### 切片 A：清單頁搜尋（Q1）
**做完你會看到**：頁首多一條搜尋列，打「止癢」或「松本清」或「ドン」都找得到，
結果跨全部 6 個分頁一起列出，每張卡片標明來自哪一頁；清空即回到原本的分頁瀏覽。
**機械驗收**：搜尋涵蓋品名／日文名／型號／分類／店家顯示名稱；結果數以 `aria-live` 播報；`npm test` 綠燈。

### 切片 B：只看未完成（Q2）
**做完你會看到**：頁首一顆「只看未完成」開關，打開後已買（或已試穿／已選定）的卡片收起來，
分區計數同步變成「剩 N 項」。開關狀態記在瀏覽器裡，下次打開還在。
**機械驗收**：狀態寫入 `localStorage`；篩到 0 項時顯示空狀態而非空白畫面。

### 切片 C：底部導覽（Q3、Q4）
**做完你會看到**：捲動時底部導覽不再透出底下的字；桌機上它變成螢幕下方置中的膠囊，不再橫跨整個畫面。
**機械驗收**：`.kij-bottombar` 背景不透明；1280px 下 `body.kij` 的導覽寬度 ≤ 480px；地圖頁維持滿版不受影響。

### 切片 D：卡片減噪（Q5、Q6）
**做完你會看到**：藥妝日用的卡片不再重複顯示分類標籤，品名有更多空間；
價格來源說明收進「價格來源說明 ▾」，要看再點開。購買提醒（`note`）維持攤開。
**機械驗收**：390px 下 `.kij-shopping-details` 值欄仍 > 160px；`npm test` 綠燈。

### 切片 E：行程頁（Q7–Q9）
**做完你會看到**：日期分頁改成與清單頁同一套膠囊，今天那顆標「今天」；
往下捲時日期分頁貼在頁面上方不會消失；手機版「導航」回到停靠點同一列。
**機械驗收**：`npm test` 綠燈；560px 下 `.stop-item` 為三欄。

### 切片 F：收尾（Q10）
**機械驗收**：選中分頁帶 `aria-current="true"`。

## 進度

- [x] 切片 A
- [x] 切片 B
- [x] 切片 C
- [x] 切片 D
- [x] 切片 E
- [x] 切片 F

## 實測結果（headless Chrome，390×844 與 1280×900）

| 量測 | 條件 | 實測 |
|---|---|---|
| 搜尋「松本清」 | 跨全部分頁 | 19 項，卡片標「藥妝日用」等來源分頁 |
| 搜尋「ジンマート」 | 日文名命中 | 1 項 |
| 搜尋「zzzz」 | 空狀態 | 結果行＋「換個關鍵字試試…」提示 |
| 只看未完成 | 勾一項後 | 41 → 40 張卡片，分區計數「剩 40 / 41 項」 |
| 只看未完成（勾選中） | 當場勾掉 | 40 → 39，卡片即時消失 |
| 篩選持久化 | `localStorage` | `kij_unbought_only = 1` |
| 切分頁 | 清掉搜尋 | 輸入框清空、`aria-current="true"` 移到新分頁 |
| 收合後頁首高度 | 390px | **116px**（門檻 ≤ 160px） |
| 搜尋列／篩選鈕高度 | 390px | 44px（`--tap-min`） |
| 明細值欄寬度 | 390px | **170px**（門檻 > 160px） |
| 底部導覽背景 | 全頁 | `rgb(255,255,255)` 不透明 |
| 底部導覽寬度 | 1280px | 清單／行程 **460px 膠囊**；地圖維持 1280px 滿版 |
| 行程日期分頁 | 捲動 700px 後 | 貼頂於 `top: 0`，仍可見 |
| 行程停靠點 | 390px | `30px 240px 42px` 三欄，導航回到同一列 |

`npm test` ✓　`npm run validate:data` ✓（75 項商品、46 家店）

## 取捨紀錄

**商品卡片的說明文字合併收合**：原本 `note` 是購買提醒（「建議帶 1 條」），
但 2026-08-22 的商品刷新讓 `PRICE_OVERRIDES` 把多數 `note` 覆寫成查證紀錄
（「照片確認為 … 官方希望零售含稅價 ¥1,320。實體藥妝的售價與庫存未確認」）。
現況下 `note` 與 `priceNote` 都是資料來源說明，兩段攤開常比價格區塊還高。
本輪把兩段合併收進「商品與價格說明 ▾」，**沒有刪掉任何文字**。
代價：少數真正的到場警語（例如「官方公告暫停販售，不列為必買現貨」）也要點一下才看得到。
若日後把購買提醒與查證紀錄分回兩個欄位，應讓購買提醒回到攤開狀態。

**「只看未完成」不藏比較表與店家橫幅**：那些是整個分區的參考資料，
與「這一項買了沒」無關；買完最後一款吹風機時把比較表一起藏掉並不合理。


---

# 全案覆審（2026-08-26，同日第二輪）

使用者要求「全案重新看過一遍」。以 headless Chrome 在 **360 / 390 / 768 / 1280** 四種寬度
自動掃描三頁（console 錯誤、橫向溢出、超出右緣的元素、觸控目標尺寸、底部導覽是否遮住內容），
再逐一手動走過互動流程，並對照基準 `deb3210` 判斷每個問題是既有的還是本輪造成的。

## 我自己改壞的（覆審才發現）

| 編號 | 問題 | 對照基準 | 修法 |
|---|---|---|---|
| R1（高）| **行程頁在 360px 橫向溢出 30px**。我把日期分頁改成 `inline-flex` + `padding: 0 15px` + `min-height: 44px` 之後，四顆膠囊的最小內容寬度超過容器；`.itinerary-main` 的 grid 子項預設 `min-width: auto`，被整個撐開 | 基準 `scrollWidth` 360 = `clientWidth`，**沒有溢出**，確定是我造成的 | `.itinerary-main > * { min-width: 0 }`。分頁列自己會橫捲，容器不該跟著長 |
| R2 | `buildCardHtml` 裡留下 `const priceNoteHtml = '';` 死變數，以及一段與新寫法互相矛盾的舊註解（還在說「priceNote 緊接在價格後面、note 放在最後」） | — | 兩者刪除 |
| R3 | `.kij-card-pricenote` 已無任何地方輸出，CSS 變成孤兒選擇器 | — | 併回 `.kij-card-note` |
| R4 | 分頁選中狀態在 `focusSection()` 與 `syncTabsForSearch()` 各寫一份，判斷條件日後會漂移 | — | 統一由 `syncTabsForSearch()` 負責 |
| R5 | 搜尋中結果跨全部分頁、分頁也不再高亮，狀態列卻仍寫「藥妝日用　已買 1 / 41」——講一個畫面上不存在的範圍 | — | 搜尋時只留全站進度 |

## 既有問題（不是本輪造成，一併修掉）

| 編號 | 問題 | 修法 |
|---|---|---|
| R6（高）| **分區收合完全沒作用**。`toggleSectionCollapse()` 設了 `hidden`、寫了 `localStorage`、把 `aria-expanded` 改成 `false`，但 `.kij-section-body` 的 `display: grid` 是 class 選擇器，優先權高過 `[hidden]` 的瀏覽器預設樣式——畫面毫無變化。等於分區標題是一顆按了沒反應的按鈕，而且對螢幕閱讀器謊報「已收合」。`map.html` 早就為 `.filter-group` 修過同一類問題，清單頁一直漏掉 | 補 `.kij-section-body[hidden] { display: none; }` |
| R7（高）| **地圖降級訊息是假的**。Leaflet 載不到時，`map-app.js` 的主體整段包在 `else` 裡被跳過，店家清單／搜尋／篩選一個都不會渲染；但畫面上寫的是「店家清單、搜尋與篩選**仍可正常使用**」。使用者會站在博多站盯著一個空白面板 | 兩條降級路徑分開講：圖磚失敗（Leaflet 有載到，清單真的能用）維持原文；Leaflet 失敗改為照實說明，並補「重新整理／回購物清單」兩顆真的能動的按鈕，同時把搜尋列、抽屜、「我在這」這些按了沒反應的假控制項收掉 |
| R8 | 360px 窄手機上卡片右欄只剩約 142px（低於本專案自訂的 160px 門檻），「松本清博多站地下街店」從店名中間斷行、只剩一個「店」字掉在下一行，價格也擠成兩行 | ≤380px 時商品圖由 156px 縮到 124px，右欄回到 176px |

## 覆審後的實測

| 量測 | 結果 |
|---|---|
| 三頁 × 360/390/768/1280 自動掃描 | console 錯誤 0、橫向溢出 0、超出右緣 0、底部導覽遮住內容 0 |
| 明細值欄寬度 | 360px→176px、375px→191px、390px→170px（門檻 > 160px） |
| 店家連結是否斷行 | 360／375／390 皆單行（25px 高） |
| 分區收合 | `aria-expanded=false` 且 `body hidden=true`，狀態一致 |
| 搜尋＋只看未完成 疊加 | 19 → 18 項，結果行「找到 18 項未完成商品」 |
| 六個分頁逐一渲染（篩選開著）| 卡片數與比較表數皆正確，計數文字與畫面一致 |
| 地圖降級：Leaflet 失敗 | 照實訊息＋兩顆動作鈕（44px），假控制項已收 |
| 地圖降級：只有圖磚失敗 | 維持原訊息，搜尋列與抽屜**不**收掉（迴歸確認） |
| Tab 走訪順序 | 搜尋 → 只看未完成 → 分頁列 |
| `npm test` / `npm run validate:data` | ✓ / ✓（75 項商品、46 家店）|

## 已知未處理

- **320px（iPhone SE 一代等）**：右欄約 136px，店家連結仍會斷成兩行。沒有橫向溢出、不影響操作，
  屬於優雅降級；本專案的版面門檻一向以 360px 為下限，不為 320px 再加一個斷點。
- **商品名連結與店家連結的可點高度**（17px／25px）低於 `--tap-min`。這是 2026-08-12 那輪
  已記錄並接受的取捨（改成膠囊標籤會吃掉窄卡片的整整一列），本輪維持原判斷。
- **地圖頁在 Leaflet 失敗時仍無法提供店家清單**。真正的修法是把店家清單的渲染從 Leaflet
  解耦出來（`renderStores` 目前與 `map`、`markers`、距離排序深度綁在一起），
  屬於獨立一輪的重構；本輪只讓訊息說實話、並給出可行的替代動作。
